#Apt Cleaner Demon without autorun#
$sudo apt update
$sudo apt autoremove
$sudo apt auto-remove
$sudo apt autopurge
$sudo apt autoclean
#$sudo apt fix-
$sudo apt clean
$sudo apt check
$sudo apt modernize-sources
$sudo apt held
#$sudo apt
$sudo apt update
$sudo apt autoremove
$sudo apt auto-remove
$sudo apt autopurge
$sudo apt autoclean
#$sudo apt fix-
$sudo apt clean
$sudo apt check
$sudo apt modernize-sources
$sudo apt held
#$sudo apt
