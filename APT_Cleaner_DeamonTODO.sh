#Apt Cleaner Demon#
#run once a week
#
#run once a day
#
#run once an hour
#
#run once a month
$sudo apt update
$sudo apt autoremove
$sudo apt auto-remove
$sudo apt autopurge
$sudo apt autoclean
#$sudo apt fix-
$sudo apt clean
$sudo apt check
$sudo apt modernize-sources
$sudo apt held
#$sudo apt
$sudo apt update
$sudo apt autoremove
$sudo apt auto-remove
$sudo apt autopurge
$sudo apt autoclean
#$sudo apt fix-
$sudo apt clean
$sudo apt check
$sudo apt modernize-sources
$sudo apt held
#$sudo apt
####What the fuck is wrong with APT working oddly with $?
sudo dpkg --configure -a
$sudo apt install -f
$sudo apt install --fix-broken
sudo dpkg --configure -a
####What the fuck is wrong with APT working oddly with $?
$sudo apt install -f
$sudo apt install --fix-broken
$sudo apt purge
####add -y and complete with error but print make deb version without the I wanan say check option?
