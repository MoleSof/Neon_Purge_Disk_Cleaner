#Apt Cleaner Demon#
#run once a week
#
#run once a day
#
#run once an hour
#
#run once a month
$sudo apt update
$sudo apt autoremove
$sudo apt auto-remove
$sudo apt autopurge
$sudo apt autoclean
#$sudo apt fix-
$sudo apt clean
$sudo apt check
$sudo apt modernize-sources
$sudo apt held
#$sudo apt
$sudo apt update
$sudo apt autoremove
$sudo apt auto-remove
$sudo apt autopurge
$sudo apt autoclean
#$sudo apt fix-
$sudo apt clean
$sudo apt check
$sudo apt modernize-sources
$sudo apt held
#$sudo apt
